
ext.hexagonVersion = findProperty ('hexagonVersion') ?: '0.9.11'

apply from: "$gradleScripts/kotlin.gradle"

defaultTasks 'build'

dependencies {
    compile "co.there4:hexagon:$hexagonVersion"

    testCompile "co.there4:hexagon:$hexagonVersion:test"
}
