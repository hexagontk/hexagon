apply(plugin: "java")

sourceSets {
    main { sourceSet ->
        java {
            setSrcDirs([sourceSet.name])
        }
        resources {
            setSrcDirs([sourceSet.name])
        }
    }
    test { sourceSet ->
        java {
            setSrcDirs([sourceSet.name])
        }
        resources {
            setSrcDirs([sourceSet.name])
        }
    }
}
