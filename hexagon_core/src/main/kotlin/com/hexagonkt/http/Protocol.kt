package com.hexagonkt.http

/**
 * Enum for the supported HTTP methods.
 */
enum class Protocol {
    /** HTTP GET method. */
    HTTP,
    /** HTTP HEAD method. */
    HTTPS,
    /** HTTP POST method. */
    HTTP2
}
